version: '3.8'
services:
  frontend:
    build: .
    ports:
      - "80:80"
    restart: unless-stopped
    volumes:
      - ./nginx.conf:/etc/nginx/nginx.conf:ro
      - ./src/assets:/usr/share/nginx/html/assets:ro
    environment:
      - NODE_ENV=production
